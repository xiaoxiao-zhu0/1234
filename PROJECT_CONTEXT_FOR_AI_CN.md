# 北斗巡知：队员与AI项目总上下文

> 本文件描述完整产品流程，但不披露私有持续学习算法内部实现。

## 1. 项目目标

项目名称为“北斗时空感知持续学习技术与低空无人机巡检系统”，简称“北斗巡知”。面向电力、交通、自然资源和园区等常态化无人机巡检场景，解决巡检中不断出现新类别目标、不同地理区域性能差异明显以及有限回放资源无法按风险配置的问题。

北斗在项目中的作用是跨架次时间与位置索引、地理区域诊断和复飞任务定位，不是给普通识别算法增加装饰性坐标。

## 2. 完整计算流程

```text
公开航拍图像
    ↓
目标裁剪与类别标注
    ↓
生成巡检顺序、时间、虚拟WGS84坐标和定位扰动
    ↓
按新类别出现顺序构建类别增量任务流
    ↓
【外部私有持续学习引擎】
    ↓
输出真实类别、预测类别和历史正确状态
    ↓
按location_id计算识别率、遗忘率和模型不稳定性
    ↓
区域风险 = 模型不稳定性 × 业务后果
    ↓
在固定总量下生成区域回放预算建议
    ↓
输出重点复飞与人工复核建议
    ↓
共享前端读取公开JSON并可视化
```

私有持续学习引擎只作为外部黑箱。当前区域预算属于诊断与调度建议，尚未反向控制私有训练缓存。共享工程不实现或推测其模型网络、损失函数、回放采样、模型权重和训练配置。

## 3. 当前完成状态

已完成：

- 北斗航迹与8个地理网格的交互仿真页；
- 定位扰动、区域诊断、风险预算和复飞队列演示；
- 公开结果JSON契约、mock数据和独立结果看板；
- 结果质量门槛：主方法未优于最佳对比方法时明确标记为内部诊断结果；
- Windows下正确支持`.mjs`的本地启动服务；
- 私有侧白名单结果导出接口；
- Python与JavaScript边界测试。
- VisDrone目标裁剪10类5任务正式实验结果，公开JSON为`prediction_backed`且通过质量门槛；
- 代表性Seed 20260803下，三层方法最终准确率43.59%，ER为31.48%。

尚未包含：

- 持续学习核心源码及模型权重；
- 真实北斗终端、真实航迹及机载部署；
- 允许队员自行启动训练的远程黑箱API。

因此，该目录的代码基本完成的是“竞赛前端、业务仿真和结果展示”，不是“完整模型训练系统”。

## 4. 页面职责

### 交互仿真页：`frontend/index.html`

展示航迹、定位扰动、区域风险、预算调度和复飞闭环。识别率、遗忘率、坐标与路线均为仿真数据，只能作为功能原型。

### 公开结果页：`frontend/dashboard.html`

依次尝试读取：

1. `data_contract/public_result.json`；
2. `data_contract/public_result.mock.json`。

只有算法负责人导出的正式JSON才能作为`prediction_backed`结果。该状态仍不代表真实北斗外业或机载部署。
此外还必须检查`quality.recommended_for_bp`：只有值为`true`的版本才可作为BP候选指标。

## 5. 文件地图

```text
beidou_competition_shared/
├─ AGENTS.md                       AI权限与保密边界
├─ 00_READ_ME_FIRST_CN.md          启动入口
├─ PROJECT_CONTEXT_FOR_AI_CN.md    项目总上下文
├─ TEAMMATE_AI_PROMPT_CN.md        队员AI提示词
├─ start_shared_demo.ps1           前台HTTP服务
├─ index.html                      根地址跳转
├─ frontend/                       页面和可公开展示逻辑
├─ data_contract/                  mock与正式结果字段契约
├─ docs/                           交接和结果导出说明
├─ assets/                         图片、曲线和截图
└─ tests/                          接口与隔离边界测试
```

## 6. AI可以优化的范围

- 信息架构、导航、页面层级和讲解顺序；
- 风险地图、方法对比、区域详情、预算图表和复飞队列；
- 加载中、无结果、成功、失败及接口异常状态；
- 响应式布局、视觉一致性、可访问性和交互动效；
- BP截图模式、图表导出和现场演示路径。

若需要新数据，AI只能提出data contract字段建议，由算法负责人决定是否加入白名单导出器；不能在前端自行补算或虚构实验结论。

## 7. 证据状态

| 状态 | 含义 | 合法表述 |
|---|---|---|
| `simulation_only` | 路线、坐标和指标均为仿真 | 功能原型、业务闭环演示 |
| `position_only` | 时空元数据已处理但没有模型预测 | 数据链路与时空诊断跑通 |
| `prediction_backed` | 指标来自模型预测文件 | 公开数据实验结果 |

任何状态都不能自动推导出真实外业、商业客户、论文发表或机载部署已经完成。

`prediction_backed`只说明数据来源真实，不说明主方法优于基线。质量字段分为：

- `competition_candidate`：主方法高于随机水平，并优于当前文件中的最佳对比方法；
- `diagnostic_only`：仅用于定位问题，不可作为BP核心指标。

## 8. AI接手后的工作顺序

1. 阅读边界和上下文；
2. 启动并检查两个页面；
3. 运行现有测试；
4. 给出界面问题与优先级；
5. 使用mock数据开发空值和异常状态，但不得覆盖正式JSON；
6. 使用算法负责人提供的正式JSON联调结果看板；
7. 保留证据状态与披露说明。
